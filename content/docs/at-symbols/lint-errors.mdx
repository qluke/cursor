---
title: "@Lint Errors"
description: "@Lint Errors"
---

## @Lint Errors

使用 **@Lint Errors** 符号访问并引用代码库中的 lint 错误。

---

### **功能说明**

- **自动捕获错误**：**@Lint Errors** 会自动捕获您当前活动文件中的所有 lint 错误和警告，并将其作为上下文提供。  
- **默认可见**：在 **Composer** 和 **Agent** 中，lint 错误会默认显示，无需额外操作。