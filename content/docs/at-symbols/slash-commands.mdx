---
title: /command
description: /command
---

## /command

使用 **/commands** 快速引用打开的编辑器标签，并将它们作为上下文添加到与 Cursor AI 的对话中。

---

### **功能说明**

- **快速引用**：通过输入 **/**，可以快速选择打开的编辑器标签并将其作为上下文。  
- **上下文选项**：  
  - **Open editors**：当前打开的所有编辑器标签。  
  - **Active editors**：当前视图中的编辑器标签（例如，在分屏显示多个编辑器时）。